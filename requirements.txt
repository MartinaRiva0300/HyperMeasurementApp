# Hyperspectral Control App — Python dependencies
#
# Install into your environment (e.g. the `scopefoundry` conda env):
#     pip install -r requirements.txt
#
# Hardware drivers required separately on Windows:
#   - Hamamatsu DCAM4 SDK (for CameraDevice.py / Orca Flash 4V3)
#   - PI GCS / pipython USB drivers (for PI_VC_device.py)

PyQt5>=5.15
numpy>=1.21
pyqtgraph>=0.12
h5py>=3.0

# PI Python wrapper — only needed for the real stage (already vendored in
# ScopeFoundry Projects/PI_ScopeFoundry/PIPython). Uncomment if not installed:
# pipython
